requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional: for browser-mode (--browser) fetching. Install separately:
#   pip install playwright && playwright install chromium
# playwright>=1.40.0
